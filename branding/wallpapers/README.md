# Wallpapers

Place default stills here. Build/install path: `/usr/share/backgrounds/codalinux/`.

`default.png` is a simple branded gradient used by `hyprpaper` on the live session. Regenerate with `python3 scripts/gen-wallpaper.py`. A greetd background hook is still later.
