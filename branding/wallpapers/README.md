# Wallpapers

Place default stills here. Build/install path: `/usr/share/backgrounds/codalinux/`.

`desktop/hypr/hyprpaper.conf` points at `default.png` once that file exists. No binary assets are committed in the scaffold.

TODO: add a default wallpaper and a greetd background hook.
