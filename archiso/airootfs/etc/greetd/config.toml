# Live ISO: greetd owns tty1 and starts Hyprland for a tryable session.
# tty2 autologins to a root console if the graphical session fails.

[terminal]
vt = 1

[default_session]
command = "agreety --cmd /usr/bin/Hyprland"
user = "greeter"

[initial_session]
command = "Hyprland"
user = "root"
